## Pinned contract-run overlay. tools/contract-test.sh installs the pristine
## reference config (tools/release/valheimone.cfg) with these keys applied on
## top, so the VO_CONTRACT enabled-module fingerprint is deterministic and
## independent of whatever config the local harness happens to have.
## [Query] stays enabled so the A2S responder start/stop path is exercised.

[Query]

Enabled = true

# ValheimOne brand assets

Final ValheimOne logo PNGs land here:

- `valheimone-landscape.png`
- `valheimone-landscape-dark.png`

`valheimone-mark.svg` is the compass mark as a currentColor vector — the same
artwork the live map's sidebar renders inline. Use it wherever the icon has to
sit on an arbitrary background.
